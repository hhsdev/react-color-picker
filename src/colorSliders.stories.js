import React from "react";
import ColorSliders from './colorSliders';
import { createUseStyles } from "react-jss";

export default { title: "Colored Sliders" };

export const colorSliders = () => <ColorSliders />;
